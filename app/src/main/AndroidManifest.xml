<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grqueiroz.lupus_tcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Topic1Activity" android:label="@string/o_que_lupus"/>
        <activity android:name=".Topic2Activity" android:label="@string/o_que_causa_lupus"/>
        <activity android:name=".Topic3Activity" android:label="@string/como_e_diagnosticado"/>
        <activity android:name=".Topic4Activity" android:label="@string/como_afeta_corpo"/>
        <activity android:name=".Topic5Activity" android:label="@string/como_vou_melhorar"/>
        <activity android:name=".Topic6Activity" android:label="@string/como_sera_dia_a_dia"/>
        <activity android:name=".Login" android:label="@string/login"/>
    </application>

</manifest>